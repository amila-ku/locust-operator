name: Go
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:

    - name: Set up Operator SDK n build
      uses: xunholy/operator-sdk-action
      with:
          image: "quay.io/amila_ku/locust-operator"
          tag: 0.4
      id: go